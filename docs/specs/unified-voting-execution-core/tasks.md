# 统一表决执行内核任务

> 业务关联：逐步交付通用表决包、冻结名册、有效票台账、结果追溯和业主大会接入。
>
> 状态：实施中
>
> 需求：[requirements.md](requirements.md) · 设计：[design.md](design.md)

## 1. 实现前核对

- [x] 核对通用议题、`t_vote_item`、分母快照、计票引擎和结果快照；
- [x] 核对业主大会表决包、送达、纸票留痕及规则快照；
- [x] 确认现有分母在结算时才冻结，且投票资格仍回读实时房屋视图；
- [x] 确认当前 `account_status` 排除逻辑缺少权威业务依据，不应进入新名册快照。

## 2. 失败测试

- [ ] 写入表决包状态机和完整依据测试；
- [ ] 写入锁定时冻结名册、非名册不得投票测试；
- [ ] 写入纸质票与线上票跨渠道唯一性测试；
- [ ] 写入结果追溯表决包和名册快照测试。

## 3. 领域与数据

- [ ] 实现 `VotingExecutionPackage` 状态机和仓储端口；
- [ ] 新增 `V3.110` 迁移，建立通用包、事项关系、名册、送达、票据留痕和审计数据；
- [ ] 在名册快照中保存代表 `opid`，并以冻结数据校验投票资格；
- [ ] 移除新内核对欠费或账号状态的无依据排除；
- [ ] 使结果快照持久化表决包、名册、方案和规则摘要。

## 4. 应用服务与接入

- [ ] 实现 `VotingExecutionService` 的创建、绑定、冻结、开始、收票、截止和结算；
- [ ] 将业主大会纸质送达和纸票写入改为通用内核；
- [ ] 阻止通用线上投票入口绕过正式表决包的渠道和名册校验；
- [ ] 清理被通用内核替代的业主大会专用票据写入代码。

## 5. 验证与收口

- [ ] 运行通用表决内核及业主大会聚焦测试；
- [ ] 运行全量 Maven 测试；
- [ ] 检查 Flyway、Markdown 链接和差异；
- [ ] 更新改造计划并以独立提交保存切片三。
