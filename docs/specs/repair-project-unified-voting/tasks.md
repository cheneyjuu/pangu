# 维修工程接入统一表决任务

> 业务关联：跟踪维修授权提案接入统一表决、三端办理和历史迁移的完成情况。
>
> 状态：统一办理、未反馈票计票和书面委托代理已完成本地回归；待部署验收（2026-07-20）

## 1. 精确范围与内核扩展

- [x] 增加 `REPAIR_ALLOCATION` 范围和数据库约束；
- [x] 增加按精确房屋集合读取候选名册的仓储能力；
- [x] 抽取统一表决的精确名册冻结入口，保留单一校验和审计链；
- [x] 校验方案分摊房屋与当前有效名册、面积和唯一代表完全一致；
- [x] 覆盖范围漂移和原子回滚测试。

## 2. 维修表决编排

- [x] 新增维修表决关联模型、仓储和迁移；
- [x] 校验项目、冻结方案、活动规则和本次方式；
- [x] 原子创建事项、表决包、精确名册和关联；
- [x] 开始、截止、结算并将通过结果写成治理依据；
- [x] 未通过时回到方案修订且保留历史记录；
- [x] 覆盖通过、未通过、并发和重复发起测试。

## 3. 管理端与业主端接口

- [x] 提供维修表决准备、开始、结算和进度接口；
- [x] 提供维修纸票办理薄适配接口；
- [x] 提供业主维修表决任务、阅读确认、投票、纸质协助和回执接口；
- [x] 确认普通管理接口不泄露个人线上选择。

## 4. 三端交互

- [x] 盘古以业务语言展示“准备相关业主表决”和渠道办理台；
- [x] 瑶池复用相同契约且不建立第二套规则；
- [x] 神农业主端展示材料、本人表决权、渠道、提交确认和回执；
- [x] 删除“定商”“当前授权状态”“服务端快照”等面向业务人员的技术文案。

## 5. 历史迁移与切换

- [x] 提供旧楼栋决定和业主大会关联的逐项目盘点脚本；
- [x] 生成迁移、只读保留或人工决定的分类报告；
- [x] 关闭旧维修决定、微信接龙和手工结果的新写入口；
- [x] 验证历史详情仍可读取且不会进入新计票。

## 6. 验证与发布

- [x] 后端全量测试通过（676 项通过，1 项跳过）；
- [x] 盘古、瑶池和神农构建通过；
- [x] SSH 部署后验证数据库迁移、健康检查和静态资源；
- [x] 在生产环境完成一条纸质、一条线上和一条混合的真实维修表决流程；
- [x] 保存验收证据并更新权威规范和任务状态。

## 7. 生产验收记录

- Pangu 发布提交：`05d5b66edbdd`；生产 JAR SHA-256：`a991d16e1415b6631bbd1fcf228d621f5d0371d619dd9070cfffa06e3b446db1`。
- 服务状态：`pangu.service` 为 `active`；Flyway 成功校验 134 个迁移，数据库版本为 `3.118`。
- Yaochi 发布目录：`/var/www/yaochi/releases/20260720011659-92c02f7e06de`；生产首页、JavaScript 和 CSS 均返回 HTTP 200。
- 验收租户和范围：测试租户 `10001`、楼栋 `30002`；验收数据均以 `PROD-ACCEPT-20260720-R3` 标识，不作为真实业主决定。

| 办理方式 | 项目编号 | 表决结果 | 项目/方案结果 |
| --- | --- | --- | --- |
| 纸质书面征询 | `RP-20260720-000009` | `PASSED` | `AUTHORIZED` / `LOCKED` |
| 线上实名表决并提供纸质协助 | `RP-20260720-000010` | `PASSED` | `AUTHORIZED` / `LOCKED` |
| 纸质与线上并行 | `RP-20260720-000011` | `PASSED` | `AUTHORIZED` / `LOCKED` |

三个项目各形成 1 条不可变计票结果，冻结名册均为 1 人、177 平方米，参与和同意均为 1 人、177 平方米。混合流程同时核验：线上票后收到的纸票保留为 `DUPLICATE`、纸票生效后再次线上提交返回 HTTP 409、管理端冲突记录不泄露个人选择；结算后仍能读取纸票原件、复核结果、重复材料记录和线上汇总。混合项目最终以报价 `12` 完成施工单位确认，授权状态为 `AUTHORIZED`。

生产历史盘点结果：真实业务租户 `20000` 现有维修项目为 `NO_VOTING_RECORD`，旧维修工单决定和旧业主大会关联均为 0 条；无法证明的历史事实没有被补造成统一有效票。测试租户内早期失败验收记录继续以 `PROD-ACCEPT` 前缀隔离，不纳入真实业务迁移。

## 8. 未反馈票可审计计票扩展

- [x] 明确有效送达、无有效票和冻结名册共同构成未反馈认定资格；
- [x] 明确多数意见按实际票的人数与面积分别计算，双维度不一致或并列时阻断；
- [x] 增加实际票与认定票分源计票模型及领域测试；
- [x] 增加逐事项未反馈认定记录、逐行摘要和聚合摘要迁移；
- [x] 在统一结算服务中接入三种未反馈认定方式并移除旧阻断；
- [x] 在结果快照、司法存证、管理端和业主端公开汇总中展示分源统计；
- [x] 补齐业主大会与维修工程的单元、集成和权限测试；
- [x] 更新瑶池办理页业务文案和结果展示，确认神农端不泄露逐户认定；
- [ ] 全量构建、SSH 部署并以非真实业务测试数据完成三种认定方式验收。

## 9. 书面委托代理与一次性就绪检查

- [x] 就绪接口一次返回全部规则缺口，不再逐项抛出晚到阻断；
- [x] 将规则中的书面委托约定识别为可执行业务能力，不强制无委托需求的业主登记；
- [x] 保存委托原件、代理人身份、原业主房屋、有效期、异人核对、撤销和审计；
- [x] 纸质送达和纸票显式绑定已确认委托，计票继续归入原业主房屋；
- [x] 瑶池办理页增加书面委托登记、核对、撤销、原件查看和代理纸票选择；
- [x] BDD 场景覆盖物业查看、业委会发起、登记人不能自审、异人核对、未使用撤销、代理纸票、业主线上票、统一结算和施工单位选择；
- [x] 后端全量测试通过（693 项，1 项跳过），瑶池类型检查和生产构建通过。
