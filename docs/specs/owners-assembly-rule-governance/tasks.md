# 业主大会议事规则版本治理任务

> 业务关联：逐项交付并验证议事规则登记、确认、启用、替代和会次快照能力。
>
> 状态：已完成（2026-07-19）
>
> 需求：[requirements.md](requirements.md) · 设计：[design.md](design.md)

## 1. 现有实现核对

- [x] 核对 `OwnersAssemblyRule`、结构化配置、字段确认和审计模型；
- [x] 核对 `V3.99`、`V3.100`、MyBatis 映射和每租户唯一有效版本约束；
- [x] 核对主任或副主任由服务端职务查询确定，客户端不传职务；
- [x] 核对正式会议从当前有效规则生成不可变快照；
- [x] 明确旧维修事项规则不在本切片自动迁移。

## 2. 回归测试补齐

- [x] 精确验证缺少任一来源页码或条款时不能提交核对；
- [x] 验证当前租户账号不能读取、核对或启用其他租户规则；
- [x] 验证未来生效版本即使全部核对完成也不能提前启用；
- [x] 复跑规则换版和已冻结会次快照不变测试。

## 3. 实现修正

- [x] 现有实现已满足新增验收场景，因此未制造额外字段、兼容分支或迁移；
- [x] 已确认无需修改历史迁移，也无需新增 `V3.110+` 迁移；
- [x] 已复核原件继续使用私有存储和短时预览，不暴露对象存储地址。

## 4. 验证与收口

- [x] 运行 `OwnersAssemblyRuleFlowTest` 和 `OwnersAssemblyFlowTest`：11 项通过；
- [x] 运行全量 `mvn test`：653 项通过，0 项失败，1 项跳过；
- [x] 将改造计划的切片二标记为完成，并记录实际验证命令；
- [x] 检查 Markdown 链接和 `git diff --check`；
- [x] 以独立提交保存切片 SPEC 和回归测试。

## 5. 实施结论

代码审计确认，规则状态机和不可变会次快照已由既有 `V3.99`、`V3.100` 及对应服务实现。本次没有为了制造代码改动而重写已正确的生产逻辑，只补充切片级 SPEC，以及缺失来源条款、跨租户访问和未来生效日期三类回归测试。
