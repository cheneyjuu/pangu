# 维修报修至工程验收全流程任务

> 业务关联：按验收规则逐个交付规则一致性、重复票裁决、验收要求和纵向 BDD。
>
> 状态：已完成（2026-07-20）

## 1. 文档基线

- [x] 明确验收终点为工程验收通过，不包含付款、公示和质保；
- [x] 编写全流程验收规则；
- [x] 编写 requirements、design、tasks 三件套；
- [x] 修订旧混合表决、维修统一表决和改造计划中的“只支持首张有效票”冲突结论；
- [x] 更新权威业务规范，明确采集方式与重复票策略是两个维度。

## 2. 规则能力一致性

- [x] 先写失败测试：`ONLINE_PREVAILS` 生效规则必须返回混合表决可办理；
- [x] 抽取规则启用和运行共用的办理方式能力策略；
- [x] 规则启用前一次返回全部不支持原因；
- [x] 删除 `FormalVotingRulePolicy` 中只接受 `FIRST_VALID_WINS` 的硬编码分支；
- [x] 验证现有测试规则无需修改即可准备表决。

## 3. 重复票策略

- [x] 以 RED-GREEN 逐个实现 `FIRST_VALID_WINS`、`ONLINE_PREVAILS`、`PAPER_PREVAILS`；
- [x] 增加有效票失效/替代审计所需迁移、领域模型和仓储契约；
- [x] 线上与纸质适配器共用裁决服务，不各自判断优先级；
- [x] 覆盖两个到达顺序、幂等和并发；
- [x] 管理端展示业务化重复票处理结果且不泄露票面。

## 4. 验收要求

- [x] 核对现有实施方案、合同和验收字段，停止按楼栋/全小区硬编码固定参与角色；
- [x] 建立带依据附件和摘要的验收要求快照；
- [x] 从锁定方案生成验收任务并一次返回全部未满足条件；
- [x] 按快照完成参与方提交、整改、新轮次和验收通过；
- [x] 确保验收通过不自动进入付款、公示或质保。

## 5. 纵向 BDD

- [x] 主场景从业主报修接口开始，到工程验收通过结束；
- [x] 全部业务结果通过公开接口形成，不直接插入结果或修改状态；
- [x] 使用测试时钟推进期限，不在测试中直接更新表决时间和状态；
- [x] 断言跨工单、工程、方案、表决、施工单位、合同、施工、结算和验收的追溯链；
- [x] 增加整改后重新验收场景和权限/隐私场景。

## 6. 三端与发布

- [x] 盘古补充准确的规则阻塞、重复票和验收办理文案；
- [x] 神农完成业主报修、投票和本人验收任务串联；
- [x] 瑶池完成报价、施工和完工材料串联；
- [x] 运行 Pangu 全量测试、Yaochi 构建、Shennong 类型检查和体验构建；
- [x] 提交精确版本并通过 SSH 部署；
- [x] 使用隔离生产测试数据跑完整主场景并保存验收证据。

## 7. 完成证据

- 生产部署、数据库迁移、三端构建和线上纵向 BDD 结果见
  [生产验收记录](./production-acceptance-20260720.md)；
- 生产纵向 BDD 由 `scripts/e2e-repair-project-acceptance.mjs` 通过公开 HTTP 接口执行；
- 验收终点为工程项目 `COMPLETED` 且验收结论 `PASSED`，未自动生成付款申请或完工公示。
